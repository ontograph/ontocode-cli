---
description: Алиас флоу “map repo → baseline Memory Bank (as-is)”. Используй /map-codebase.
status: active
---
# /mb-map-codebase — Alias

<objective>
Синоним для “замаппить существующий код в Memory Bank как есть”.
</objective>

<process>
Используй `/map-codebase`.

Важно:
- если **нет PRD**, это только “as-is” документация (без EP/FT/TASK)
- после маппинга попроси PRD/delta и только затем запускай `/prd`
</process>

