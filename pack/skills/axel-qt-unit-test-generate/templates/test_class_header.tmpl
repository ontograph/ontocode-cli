#ifndef TEST_{CLASS_NAME_UPPER}_H
#define TEST_{CLASS_NAME_UPPER}_H

#include <QtTest/QtTest>
#include "{HEADER_FILE}"

class Test{CLASS_NAME} : public QObject
{{
    Q_OBJECT

private slots:
    // ── 夹具 ──────────────────────────────────────────
    void initTestCase();
    void cleanupTestCase();
    void init();
    void cleanup();

    // ── 测试函数 (按级别) ──────────────────────────────
{TEST_DECLARATIONS}
}};

#endif // TEST_{CLASS_NAME_UPPER}_H
