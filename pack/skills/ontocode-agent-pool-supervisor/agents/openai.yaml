interface:
  display_name: "Ontocode Agent Pool Supervisor"
  short_description: "Drain and close healthy sub-agent pools"
  default_prompt: "Use $ontocode-agent-pool-supervisor to drain this healthy agent pool and consolidate its terminal results and receipts."
