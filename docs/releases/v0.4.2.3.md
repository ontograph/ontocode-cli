# Ontocode CLI 0.4.2.3

This internal development release is a fresh August 18 build of the current
committed source. Its Cargo package version is `0.4.2+3`; the CLI and other
machine-readable version surfaces report `0.4.2+3`, while the GitHub tag,
installer input, and release assets use `0.4.2.3`.

## Included Assets

- Linux x86_64 unsigned `ontocode` binary
- `SHA256SUMS`
- `install-ontocode-linux-x64.sh`
- `install-ontocode-linux-x64-v0.4.2.3.sh`
- `PROJECT_PLAN_AUTHORING_TEMPLATE.md`
- `release-notes-v0.4.2.3.md`
- curated `ontocode-content-pack-0.4.2.3.tar.gz`
- `ontocode-content-pack-0.4.2.3.manifest.json`
- `install-content-pack.sh`

## Scope

- rebuilds the current committed source on August 18, 2026
- includes the manager-loop retry fixes published in `0.4.2.2`
- excludes unrelated uncommitted changes from the primary development checkout
- preserves every existing release tag and asset
- publishes repository-owned build and release-publication skills as an
  optional, non-overwriting home or project content pack

## Verification

The release binary reports `Ontocode CLI 0.4.2+3`. All nine release assets are
covered by `SHA256SUMS`, the public-reference leak scan passes, and the content
pack has been installed from staged assets into temporary home and project
roots with overwrite refusal and path containment verified.
